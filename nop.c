#include "monty.h"
/**
  *nop- function to do nothing
  *@head: stack head pointer
  *@count: number line
  *Return: no return
 */
void nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
